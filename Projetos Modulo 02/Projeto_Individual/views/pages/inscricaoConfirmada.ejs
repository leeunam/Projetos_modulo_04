<!DOCTYPE html>
<html lang="pt-br">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="/style.css" rel="stylesheet" />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;700;800&family=Plus+Jakarta+Sans:wght@400;500;700&display=swap"
      rel="stylesheet"
    />
    <title>Simples - Inscrição Confirmada</title>
  </head>
  <body style="padding: 0; align-items: flex-start; background: white">
    <div class="page-container">
      <div id="header-placeholder"></div>

      <div class="main-content">
        <div class="main-layout">
          <div class="confirmation-content">
            <section class="confirmation-message">
              <h1 class="confirmation-title">Inscrição Confirmada!</h1>
              <p class="confirmation-text">
                Você está pronto para participar do "<%= event.name %>".
                Enviamos um e-mail de confirmação para o seu endereço
                cadastrado. Verifique sua caixa de entrada para mais detalhes.
              </p>
            </section>

            <section class="event-details">
              <h2 class="section-header">Detalhes do Evento</h2>

              <div class="details-grid">
                <div class="details-row">
                  <div class="detail-item">
                    <div class="detail-label">Nome do Evento</div>
                    <div class="detail-value"><%= event.name %></div>
                  </div>
                  <div class="detail-item">
                    <div class="detail-label">Data</div>
                    <div class="detail-value">
                      <%= new Date(event.event_date).toLocaleDateString('pt-BR',
                      { day: '2-digit', month: 'long', year: 'numeric' }) %>
                    </div>
                  </div>
                </div>
                <div class="details-row">
                  <div class="detail-item">
                    <div class="detail-label">Horário</div>
                    <div class="detail-value"><%= event.event_time %></div>
                  </div>
                  <div class="detail-item">
                    <div class="detail-label">Localização</div>
                    <div class="detail-value">
                      <%= event.district %>, <%= event.cep %>
                    </div>
                  </div>
                </div>
              </div>
            </section>

            <section class="event-image-section">
              <div class="confirmation-image">
                <div class="image-container">
                  <div class="full-image">
                    <img
                      src="https://images.unsplash.com/photo-1540575467063-178a50c2df87?ixlib=rb-4.0.3&auto=format&fit=crop&w=1200&q=80"
                      alt="<%= event.name %>"
                      style="width: 100%; height: 100%; object-fit: cover"
                    />
                  </div>
                </div>
              </div>
            </section>

            <section class="action-section">
              <div class="registration-section">
                <button
                  class="register-btn"
                  onclick="window.location.href='/eventos'"
                >
                  Explorar Mais Eventos
                </button>
              </div>
            </section>
          </div>
        </div>
      </div>
    </div>

    <script src="/scripts.js"></script>
  </body>
</html>
